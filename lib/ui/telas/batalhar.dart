import 'package:flutter/material.dart';

class Batalhar extends StatelessWidget {
  const Batalhar({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}